name: Deploy to Localhost

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: Show Files
        run: ls -la

      - name: Print Message
        run: echo "Hello from GitHub Actions!"
